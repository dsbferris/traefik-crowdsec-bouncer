api:
  insecure: true

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: "test-net"

entryPoints:
  web:
    address: ":80"

# log:
#   level: "DEBUG"